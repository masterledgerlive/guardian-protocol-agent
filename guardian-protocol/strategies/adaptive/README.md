# Adaptive strategies (Sprint 3)

`ADAPTIVE-0.1` — scales chunk size with object length; Immediate lane for tiny jobs.

Also registered:

- `COSTOPT-0.1` — treasury-aware chunk/redundancy
- `PRIORITY-0.1` — Immediate lane for critical/small jobs
- `REDOPT-0.1` — higher redundancy for long-tail objects

Beat `FIFO-0.1` with evidence before replacing the baseline.
