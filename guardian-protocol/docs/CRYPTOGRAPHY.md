# Cryptography

Cryptography-agile. Candidates may include conventional hash commitments, Merkle trees, XMSS, SPHINCS+, ML-DSA, future PQ constructions.

Each module is benchmarked for security assumptions, proof/signature size, computation, verification cost, storage overhead, bandwidth, operational complexity, migration strategy.

Sprint 1 uses SHA-256 for object and chunk commitments only.
