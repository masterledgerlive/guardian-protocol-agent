# Agents — GRAFT offshoot on-ramp

You are working on **GRAFT**, an isolated prompt-trial nursery.

Do **not** edit `vita/`, root `agent.js`, `public/vita.html`, `piggy-bank.js`,
or `guardian-v4/` from GRAFT work.

## Read order

1. `graft/FORMULA.md` — invariants
2. `graft/README.md` — run / Telegram / commands
3. `graft/memory/offshoot-graft-brief.md` — the offshoot of the LLM idea
4. `graft/store.js` · `graft/think.js` · `graft/commands.js`

## Mission

- Keep taking architecture prompts (`/graft insert` or `--cmd`).
- Never delete raw artifacts. Show them with `/graft raw` + cas:// loc.
- Inject rooted libraries (`/graft inject local|github`), snark-compress
  (`/graft snark code`), activate without GitHub from local CAS.
- Think in the open. Prefer the most rooted. Charge the GRAFT piggy.
- Last-root + short `§GRAFT§` / `§GRAFTSNARK§` tags only — never invent tx hashes.
- Harvest is a mark, not a VITA write.

## Merge stance

GRAFT stays an offshoot until a human harvests a survivor into the VITA
avenue. Do not fork a second trader. Do not infect VITA HTML from here.
