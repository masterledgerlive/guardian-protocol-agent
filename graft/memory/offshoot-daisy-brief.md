# DAISY offshoot brief — GRAFT-local 5-layer map

The LLM dump wants Polkadot JAM coretime, Virtuals IAOs, ACP escrow, Pinecone,
Render/Akash GPU hire, and post-quantum Rust agents as one "All-in-One machine."

**DAISY is the offshoot of that idea inside GRAFT.** We map the five layers onto
this nursery so you can file, activate, and fund them *without* touching VITA
or building those networks.

| Layer | LLM dump | GRAFT / DAISY offshoot (what you can activate now) |
|---|---|---|
| L0 Compute | Buy JAM coretime, ZK on many cores | Paid `/graft think` cycles. Piggy buys "coretime" locally. |
| L1 Economy | Base smart wallet + IAO token | GRAFT piggy. `/graft fund`. Never V3 piggy, never an IAO. |
| L2 Commerce | ACP RFC / escrow / swarm hire | Filed ideas stay separate. Later: one active idea may *mark* another as hired. No escrow contract here. |
| L3 Mind | G.A.M.E. + vector DB squash | CAS + thought traces. Originals never deleted. Derived briefs are the squash. |
| L4 Senses | AgentKit / Twitter / GitHub / Telegram | Dedicated GRAFT Telegram + `--cmd` inlet. |

## What we refuse (until data says it survived)

- Do not deploy JAM, mint an agent token, or wire Render/Akash from this folder.
- Do not import `vita/`, `agent.js`, or Guardian V4.
- Do not invent tx hashes or claim Base/Polkadot settlement that did not happen.
- Recursive memory for VITA stays on the VITA avenue. DAISY only proves whether
  *this filing/activation loop* is worth harvesting later.

## Activate this dump

```
/graft list
/graft activate DAISY
/graft fund 0.001
/graft think last
/graft log last
```

If the thought log + last-root look useful after you throw money at it, mark
`/graft survive` or `/graft harvest`. Harvest still does not write VITA.
