# GRAFT offshoot — raw + loc + snark library inject

Operators could not see **raw data** or a **location**. Fix:

1. Every artifact stamps `cas://sha256:<id>` + `§GRAFTSNARK§` on file.
2. `/graft raw <id>` prints RAW DATA + loc + snark.
3. `/graft inject local` (or `github`) files rooted `graft/*` into CAS.
4. `/graft snark code` snark-compresses the whole library.
5. `/graft activate` + `/graft think` read CAS only — **activate without GitHub**.
6. Think blends `rootedPrefer` so agentic edits orbit the most rooted code.

Chain loc stays empty until human harvest into VITA. Never invent tx hashes.
